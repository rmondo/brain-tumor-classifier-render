# brain-tumor-classifier-render
Repo receives model created in Brain-tumor-classifier: Runs the same Flask inference server from section 18 of Brain-tumor-classifier notebook: serves `GET /` → drag-and-drop MRI upload UI; handles `POST /predict` → returns predicted class + all probabilities as JSON; exposes `GET /health` → liveness check.
